#ifndef NOACOMPILER_HASHMATH_H
#define NOACOMPILER_HASHMATH_H

#include "type.h"
//用于将字符串转化为指定范围内的哈希函数
uint8 HashCode8(char* key);
uint32 HashCode(char * key);
bool isNum(char* str);

#endif // !NOACOMPILER HASHMATH

